# Speedtest CLI Placeholder

Place the official Ookla Speedtest CLI binary here when packaging the app:

```text
tools/speedtest/speedtest.exe
```

The app also works if `speedtest` is installed on PATH. Review Ookla's EULA and redistribution terms before committing or shipping the binary with PingerApp.
